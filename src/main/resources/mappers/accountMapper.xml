<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.deataworld.accountMapper">
	<select id="r_nickChk" parameterType="String" resultType="java.lang.Integer">
		select sum(cnt) from(
			select count(*) as cnt
			from daetaworld.employer as r 
			where r.r_nick = #{r_nick}
			union
			select count(*) as cnt
			from daetaworld.employee as e
			where e.e_nick = #{r_nick}
		)tbl 
  	</select>
</mapper>