<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.deataworld.employee">

<!-- 내 정보 불러오기(구직자) -->
<select id="myInfo" resultType="MemberDTO">
	SELECT * FROM daetaworld.member
	WHERE m_type = '1' and m_id = 'user';
</select>


<!-- 개인정보 수정시 패스워드 검증 (구직자) -->
<select id="m_passWord" parameterType="String" resultType="String">
	select m_password
	from daetaworld.member
	where m_password = #{m_password} and 
</select>


<!-- 구직자 회원정보 수정 -->
<update id="m_myInfoUpdate" parameterType="MemberDTO">
	UPDATE daetaworld.member
	SET
	m_nick = #{m_nick},
	m_phone = #{m_phone},
	m_address1 = #{m_address1},
	m_address2 = #{m_address2},
	m_picture = #{m_picture},
	m_email = #{m_email},
	m_terms1 = #{m_terms1},
	m_terms2 = #{m_terms2},
	m_regdate = now(),
	WHERE e_number = #{m_number}; 
</update>




</mapper>
